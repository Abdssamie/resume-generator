import type { Metadata } from "next";
import { Geist, Geist_Mono, Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ['latin'], variable: '--font-sans' });

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Free Resume Generator | Powered by RenderCV",
  description: "Create professional, ATS-friendly resumes in minutes using the powerful RenderCV engine. Exports to PDF and YAML.",
  keywords: ["resume", "cv", "generator", "builder", "latex", "rendercv", "free", "open source"],
  openGraph: {
    title: "Free Resume Generator | Powered by RenderCV",
    description: "Create professional, ATS-friendly resumes in minutes.",
    type: "website",
    locale: "en_US",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
