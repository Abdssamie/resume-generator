# API Configuration
# Comma-separated list of origins allowed to call the API (CORS)
ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend.vercel.app

# Comma-separated list of hosts this API is allowed to serve (Host Header)
# Add your production domains here to prevent "Invalid host header" errors
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Secret key for API authentication (Must match NEXT_PUBLIC_API_SECRET)
API_SECRET=your-secure-secret-here